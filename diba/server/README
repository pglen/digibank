                                    README
                                    
   This is the client / server network interaction for  DIBA. The server
listens on port 6789, and the client may connect.

  The various stages of connect are:
    
    dibacli_ping.exe        Just to see if the server is alive
    dibacli_key.exe         Key exchange, send assym key
    dibacli_sess.exe        Estabilish encrypted session
    dibacli_hello.exe       Simple null query and response
    dibacli_xxxx.exe   
                

  Files:
  
    dibaserv.exe            Server, listening to port 6789
    dibaworker.exe          Connection worker for clients
    dibaquery.exe           Example query generator
    dibaclient.exe          Example client (see stages of communication)
        
    
